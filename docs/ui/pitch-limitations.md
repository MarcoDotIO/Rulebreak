# Pitch limitations page (Candidate A)

**Status:** Demo freeze view on forensic ledger UI  
**Nav:** AppShell → **Pitch limitations** (`pitch`)  
**Source of truth:** `apps/web/src/views/pitchLimitations.ts` + `PitchLimitations.tsx`

Locks what we can honestly sell now. Caps include offline P0 as demo center of gravity; Thor #48 evidence Done but pitch not closed; SSH≠G4; not AgenC dual sessions; M13 Partial-on-UI-SSH; G4-P3/P4 Not run; paid cloud $0; #50 enablement ≠ closed live pitch; no “secure” badge / `blocked_as_expected` ≠ secure.

Archivist: prefer this UI copy over marketing paraphrases. Keep docs pointers short.
